<form method="post" action="{{ path('general_information_delete', {'id': general_information.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ general_information.id) }}">
    <button class="btn">Delete</button>
</form>
