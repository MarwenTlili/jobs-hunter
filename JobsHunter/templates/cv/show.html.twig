{% extends 'base.html.twig' %}

{% block title %}CV{% endblock %}

{% block body %}
    <h1>CV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>EducationLevel</th>
                <td>{{ cv.educationLevel }}</td>
            </tr>
            <tr>
                <th>YearsOfExperience</th>
                <td>{{ cv.yearsOfExperience }}</td>
            </tr>
            <tr>
                <th>LastUpdateAt</th>
                <td>{{ cv.lastUpdateAt ? cv.lastUpdateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Views</th>
                <td>{{ cv.views }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cv_index') }}">back to list</a>

    <a href="{{ path('cv_edit', {'id': cv.id}) }}">edit</a>

    {{ include('cv/_delete_form.html.twig') }}
{% endblock %}
