<form method="post" action="{{ path('training_delete', {'slug': training.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
    <button class="btn">Delete</button>
</form>
