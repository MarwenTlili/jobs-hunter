<form method="post" action="{{ path('professional_information_delete', {'id': professional_information.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional_information.id) }}">
    <button class="btn">Delete</button>
</form>
