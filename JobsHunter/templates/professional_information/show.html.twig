{% extends 'base.html.twig' %}

{% block title %}ProfessionalInformation{% endblock %}

{% block body %}
    <h1>ProfessionalInformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professional_information.id }}</td>
            </tr>
            <tr>
                <th>Languages</th>
                <td>{{ professional_information.languages }}</td>
            </tr>
            <tr>
                <th>Skills</th>
                <td>{{ professional_information.skills }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('professional_information_index') }}">back to list</a>

    <a href="{{ path('professional_information_edit', {'id': professional_information.id}) }}">edit</a>

    {{ include('professional_information/_delete_form.html.twig') }}
{% endblock %}
