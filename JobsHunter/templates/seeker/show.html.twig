{% extends 'layout.html.twig' %}

{% block title %}Seeker{% endblock %}

{% block body %}
    <h1>Seeker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seeker.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ seeker.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ seeker.lastName }}</td>
            </tr>
            <tr>
                <th>Civility</th>
                <td>{{ seeker.civility }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ seeker.birthDate ? seeker.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            {# <tr>
                <th>City</th>
                <td>{{ seeker.city }}</td>
            </tr> #}
        </tbody>
    </table>

    <a href="{{ path('seeker_index') }}">back to list</a>

    {# <a href="{{ path('seeker_edit', {'id': seeker.id}) }}">edit</a> #}

    {# {{ include('seeker/_delete_form.html.twig') }} #}
{% endblock %}
