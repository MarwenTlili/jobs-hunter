<form method="post" action="{{ path('professional_interest_delete', {'id': professional_interest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professional_interest.id) }}">
    <button class="btn">Delete</button>
</form>
